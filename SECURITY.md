# Security Policy

This is a demonstration project and must never contain real production secrets or personal data.

## Secrets

- Keep credentials in local environment variables.
- Never commit `.env`.
- Use `.env.example` only for placeholder configuration.
- Rotate any credential immediately if it is accidentally committed.

## Reporting

If you notice a security issue in this demo, open a GitHub issue without including sensitive exploit details or credentials.
